<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.demanganesia.banksampah">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BankSampah">
        <activity android:name=".DetailBankSampahActivity"></activity>
        <activity android:name=".Lokasi" />
        <activity android:name=".FilterActivity" />
        <activity android:name=".DaftarHargaActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".ProfilActivity" />
        <activity android:name=".DaftarActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".MasukActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>